# Benchmark-only dependencies. These are intentionally absent from setup.py.
numpy>=1.26,<2
netCDF4>=1.7,<2
psutil>=5,<8
pyarrow>=18,<25
# Zarr 3.1 requires NumPy 2; 3.0 keeps this benchmark compatible with the
# project's current NumPy 1.x environment while retaining the v3 format.
zarr>=3.0,<3.1
